# Leave true for the complete, zero-dependency hackathon demo.
# Set false only after replacing the documented testnet integration seam in
# src/services/chain.ts with a configured MidnightChainService.
VITE_DEMO_MODE=true
